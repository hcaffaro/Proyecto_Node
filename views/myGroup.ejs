<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/reset.css">
    <link rel="stylesheet" href="css/gruposStyle.css">
    <link rel="stylesheet" href="css/headerStyle.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="icon" href="img/logo-grupo.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand&display=swap" rel="stylesheet">
    <title>Mi Grupo</title>
</head>

<body>
    <header></header>
    <main>
        <section class="secciones-gruposConformados" id="seccion-grupos-conformados">
            <div>
                <br>
                <h2 class="seccion1-h2">Mi Grupo</h2>
                <br><br>
            </div>
            <div class="row row-cols-1 row-cols-md-2 g-4">
                <div class="col">
                    <div class="card" style="max-width: 450px;">
                        <div style="max-height: 120px;">
                            <div class="row row-cols-sm-2 row-cols-md-4" style="margin: 5px;">
                                <% if (group.student1_photo) { %>
                                    <img class="imgGrupo" src="img/<%= group.student1_photo %>" alt="Alumno 1">
                                <% } %>
                                <% if (group.student2_photo) { %>
                                    <img class="imgGrupo" src="img/<%= group.student2_photo %>" alt="Alumno 2">
                                <% } %>
                                <% if (group.student3_photo) { %>
                                    <img class="imgGrupo" src="img/<%= group.student3_photo %>" alt="Alumno 3">
                                <% } %>
                                <% if (group.student4_photo) { %>
                                    <img class="imgGrupo" src="img/<%= group.student4_photo %>" alt="Alumno 4">
                                <% } %>
                            </div>
                        </div>
                        <div class="card-body">
                            <form action="/updateGroup" method="POST">
                                <input type="hidden" name="id_group" value="<%= group.id_group %>">
                                <input type="hidden" name="email" value="<%= email %>">
                                <div class="mb-3">
                                    <label for="name_group" class="form-label">Nombre del Grupo</label>
                                    <input type="text" class="form-control" id="name_group" name="name_group" value="<%= group.name_group %>">
                                </div>
                                <div class="mb-3">
                                    <label for="commission" class="form-label">Comisión</label>
                                    <input type="text" class="form-control" id="commission" name="commission" value="<%= group.commission %>" readonly>
                                </div>
                                <div class="mb-3">
                                    <label for="repos" class="form-label">Repositorio</label>
                                    <input type="text" class="form-control" id="repos" name="repos" value="<%= group.repos %>">
                                </div>
                                <div class="mb-3">
                                    <label for="info" class="form-label">Descripción</label>
                                    <textarea class="form-control" id="info" name="info" rows="3"><%= group.info %></textarea>
                                </div>
                                <button type="submit" class="btn btn-primary">Guardar Cambios</button>
                            </form>
                            <div class="collapse" id="collapseGroup<%= group.id_group %>">
                                <div class="card card-body">
                                    <ul>
                                        <% if (group.student1_name) { %>
                                            <li>
                                                <%= group.student1_name %> <%= group.student1_surname %>
                                            </li>
                                        <% } %>
                                        <% if (group.student2_name) { %>
                                            <li>
                                                <%= group.student2_name %> <%= group.student2_surname %>
                                            </li>
                                        <% } %>
                                        <% if (group.student3_name) { %>
                                            <li>
                                                <%= group.student3_name %> <%= group.student3_surname %>
                                            </li>
                                        <% } %>
                                        <% if (group.student4_name) { %>
                                            <li>
                                                <%= group.student4_name %> <%= group.student4_surname %>
                                            </li>
                                        <% } %>
                                    </ul>
                                </div>
                            </div>
                            <p class="d-inline-flex gap-1">
                                <button class="btn btn-primary" type="button" data-bs-toggle="collapse"
                                    data-bs-target="#collapseGroup<%= group.id_group %>" aria-expanded="false"
                                    aria-controls="collapseGroup<%= group.id_group %>">
                                    Ver lista de alumnos
                                </button>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
    <footer></footer>
    <script src="js/headerFooter.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>
